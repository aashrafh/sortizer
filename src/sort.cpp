#include <iostream>
#include <string>
#include "./Sortizer.hpp"
using std::string;

int main(int argc, char **argv){
    string algorithmNumber = argv[1];
    string inputPath = argv[2];
    string outputPath = argv[3];
    string statsPath = argv[4];

    // Sortizer sortizer;
    // sortizer.sort(vector<long long int>());
    return 0;
}